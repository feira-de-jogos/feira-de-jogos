# TSDB to CSV

Para rodar a aplicação, é preciso criar o arquivo de variáveis de ambiente:

- `INFLUX_TOKEN`: token de acesso ao InfluxDB.
- `INFLUX_URL`: URL de acesso. Padrão: `https://tsdb.feira-de-jogos.dev.br`.
- `INFLUX_ORG`: organização do InfluxDB. Padrão: `feira`.
```
